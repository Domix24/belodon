defmodule AletopeltaTest.Year2024.Day09 do
  use ExUnit.Case, async: true
  alias Aletopelta.Year2024.Day09, as: Solution

  test "is part1 working" do
    assert Belodon.solve(Solution, :part1) == 0
  end

  test "is part2 working" do
    assert Belodon.solve(Solution, :part2) == 0
  end

  test "is part working" do
    input = ~s"""
    """

    assert Belodon.test(Solution, input, :part1) == 0
  end
end
